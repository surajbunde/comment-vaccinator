{
  "name": "comment-vaccinator",
  "version": "1.3.0",
  "description": "YouTube comment filter extension",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node build.cjs",
    "test": "node --test tests/**/*.test.js",
    "test:legacy": "node test-date-filter.js"
  },
  "devDependencies": {
    "esbuild": "^0.20.0"
  }
}
